spring.application.name=auth-api
server.port=8080

# Configuração do MongoDB
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=auth_db

# Configuração do JWT (caso queira definir um segredo fixo)
jwt.secret=chave-super-secreta
jwt.expiration=86400000  # 1 dia em milissegundos